import { incorporationEmailContent } from "@/lib/constants";
import IncorporationWrapper from "../Wrapper";

const Admin_Incorporation_Email_Page = () => {
  return <IncorporationWrapper name="Email" content={incorporationEmailContent} />;
};

export default Admin_Incorporation_Email_Page;
